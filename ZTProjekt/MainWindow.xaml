<Window x:Class="ZTProjekt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZTProjekt"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="1" Grid.Row="0" Content="Button" HorizontalAlignment="Right" Margin="0,0,35,45" VerticalAlignment="Bottom" Width="75" Command="{Binding MyButtonCommand}"/>

        <TextBox Grid.Column="1" Grid.Row="1" HorizontalAlignment="Right" Height="23" Margin="0,0,21,87" TextWrapping="Wrap" VerticalAlignment="Bottom" Width="120" Text="{Binding MyTextBox}"/>
    </Grid>
</Window>
